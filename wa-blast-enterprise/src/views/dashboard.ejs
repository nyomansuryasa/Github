<!DOCTYPE html>
<html>
<head>
    <title>WA Multi-Session Dashboard</title>
    <style>
        body { font-family: Arial; }
        table { border-collapse: collapse; width: 60%; margin: 20px auto; }
        th, td { padding: 8px 12px; border: 1px solid #ddd; }
        th { background: #f2f2f2; }
    </style>
</head>
<body>
    <h1 style="text-align:center;">📊 WA Multi-Session Dashboard</h1>

    <form action="/import" method="post" style="text-align:center;">
        <button type="submit">Import CSV</button>
    </form>

    <% for (let session in stats) { %>
        <h2 style="text-align:center;"><%= session %></h2>
        <table>
            <tr><th>Total</th><th>Sent</th><th>Failed</th><th>Pending</th></tr>
            <tr>
                <td><%= stats[session].total %></td>
                <td><%= stats[session].sent %></td>
                <td><%= stats[session].failed %></td>
                <td><%= stats[session].pending %></td>
            </tr>
        </table>
    <% } %>
</body>
</html>
